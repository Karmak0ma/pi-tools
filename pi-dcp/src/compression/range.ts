import type { ContextSnapshot, ProtocolUnit } from "../identity/types.ts";
import type { ReducedState } from "../state/reducer.ts";
export interface ResolvedRange { start: number; end: number; directUnitIndexes: number[]; consumedBlockIds: string[]; }
export type RangeResult = { ok: true; ranges: ResolvedRange[] } | { ok: false; reason: "range_invalid" | "range_overlap" | "block_partial" };
export function resolveCompressionRanges(snapshot: ContextSnapshot, state: ReducedState, ranges: readonly { startId: string; endId: string }[]): RangeResult { const resolved: ResolvedRange[] = []; for (const range of ranges) { const start = aliasBoundary(snapshot, state, range.startId, false); const end = aliasBoundary(snapshot, state, range.endId, true); if (start === undefined || end === undefined || start > end) return { ok: false, reason: "range_invalid" }; const directUnitIndexes = [...Array(end - start + 1)].map((_, offset) => start + offset); const selected = activeBlocksInRange(snapshot, state, start, end); if (selected.partial.length) return { ok: false, reason: "block_partial" }; resolved.push({ start, end, directUnitIndexes, consumedBlockIds: selected.whole }); } resolved.sort((a, b) => a.start - b.start); for (let i = 1; i < resolved.length; i++) if (resolved[i].start <= resolved[i - 1].end) return { ok: false, reason: "range_overlap" }; return { ok: true, ranges: resolved }; }
function aliasBoundary(snapshot: ContextSnapshot, state: ReducedState, alias: string, end: boolean): number | undefined { const unit = snapshot.unitAliases.get(alias); if (unit !== undefined) return unit; const blockAlias = snapshot.blockAliases.get(alias); if (!blockAlias) return undefined; const block = state.blocks.get(blockAlias.blockId); if (!block || !block.active || !block.available) return undefined; const indexes = snapshot.units.map((candidate, index) => block.coverage.effectiveEntryIds.some((id) => candidate.entryIds.includes(id)) ? index : -1).filter((index) => index >= 0); return indexes.length ? (end ? indexes.at(-1) : indexes[0]) : undefined; }
function activeBlocksInRange(snapshot: ContextSnapshot, state: ReducedState, start: number, end: number): { whole: string[]; partial: string[] } { const whole: string[] = []; const partial: string[] = []; for (const [id, block] of state.blocks) { if (!block.active || !block.available) continue; const indexes = snapshot.units.map((unit, index) => block.coverage.effectiveEntryIds.some((entryId) => unit.entryIds.includes(entryId)) ? index : -1).filter((index) => index >= 0); if (!indexes.length) continue; const first = Math.min(...indexes); const last = Math.max(...indexes); if (last < start || first > end) continue; if (first >= start && last <= end) whole.push(id); else partial.push(id); } return { whole, partial }; }
export function unitRange(snapshot: ContextSnapshot, ranges: readonly ResolvedRange[]): ProtocolUnit[] { return ranges.flatMap((range) => snapshot.units.slice(range.start, range.end + 1)); }
