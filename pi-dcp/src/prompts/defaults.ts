export const SYSTEM_GUIDANCE = `pi-dcp context guidance: local mNNNN labels identify protocol units and bNNNN labels identify active summaries. Select contiguous complete protocol units whose work is finished or resolved. Preserve decisions, constraints, exact details still needed, unresolved work, active work, pending tool exchanges, and protected content. Nested summaries use each selected bNNNN placeholder exactly once. Compression is model-authored through the compress tool; its request is authorized from the assistant response provenance, not from a model-supplied snapshot ID.`;
