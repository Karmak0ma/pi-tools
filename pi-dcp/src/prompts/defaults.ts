export const SYSTEM_GUIDANCE = `pi-dcp context guidance (append-only): manage context continuously and preserve semantic closure when selecting compression ranges. A closed range is older conversation whose work is finished or resolved and whose raw detail is no longer needed immediately—for example, completed and verified implementation, concluded research, exhausted exploration, or dead-end noise. Select it when the information can safely become a faithful summary. Do not wait for an entire task or chapter to finish if an earlier section is already closed. Keep active work, unresolved questions, still-needed exact code/error output, pending tool exchanges, and protected recent turns/tools/paths/user messages uncompressed. Use only current mNNNN protocol-unit aliases and bNNNN active-block aliases from the latest metadata. Select contiguous complete units. Summaries must faithfully preserve decisions, constraints, commands, outputs, and unresolved work. Nested summaries use each selected (bNNNN) placeholder exactly once. The snapshot is short-lived; refresh aliases rather than guessing. Compression is model-authored through the compress tool. In manual mode, compress only when the current manual request permits it.`;
