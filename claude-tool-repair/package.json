{
  "name": "claude-tool-repair",
  "version": "0.1.0",
  "description": "Repair Claude pseudo-XML tool calls that would otherwise stall Pi turns.",
  "private": true,
  "type": "module",
  "keywords": [
    "pi-extension",
    "claude",
    "tool-call-repair"
  ],
  "scripts": {
    "test": "node --test repair.test.ts",
    "typecheck": "tsc --noEmit --allowImportingTsExtensions --target ES2022 --module NodeNext --moduleResolution NodeNext --skipLibCheck index.ts fixtures.ts repair.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
